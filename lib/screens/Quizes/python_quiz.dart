import 'package:flutter/material.dart';

class PythonQuizPage extends StatelessWidget {
  const PythonQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Python'),),
    );
  }
}