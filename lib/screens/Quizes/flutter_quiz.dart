import 'package:flutter/material.dart';

class FlutterQuizPage extends StatelessWidget {
  const FlutterQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Flutter'),),
    );
  }
}