import 'package:flutter/material.dart';

class CQuizPage extends StatelessWidget {
  const CQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('C++'),),
    );
  }
}