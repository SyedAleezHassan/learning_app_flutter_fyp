import 'package:flutter/material.dart';

class ReactQuizPage extends StatelessWidget {
  const ReactQuizPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('React'),),
    );
  }
}